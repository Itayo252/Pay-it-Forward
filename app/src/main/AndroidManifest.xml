<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:fitsSystemWindows="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PayitForward.NoActionBar">
        <activity
            android:name=".Activities.NetworkActivity"
            android:exported="true"/>
        <activity
            android:name=".Activities.PendingFriendsActivity"
            android:exported="true" />
        <activity
            android:name=".Activities.FriendsActivity"
            android:exported="true" />
        <activity
            android:name=".Activities.TransferActivity"
            android:exported="true" />
        <activity
            android:name=".Activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Activities.SignupActivity"
            android:exported="true"
            android:label="@string/title_activity_signup" /> <!-- android:theme="@style/Theme.PayitForward.NoActionBar" -->
        <activity
            android:name=".Activities.SigninActivity"
            android:exported="true" />
    </application>

</manifest>